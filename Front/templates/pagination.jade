//
   Created by dtysky on 16/3/2.

div
    - for(var index = 0; index < max_index; index++){
        - if(name === undefined)
            a(href= "/" + type + "/" + index) #{index}
        - else
            a(href= "/" + type + "/" + name + "/" + index) #{index}
    - }